/// Tauri app permissions — simplified default without schema dependency for first boot
{
  "identifier": "default",
  "description": "Default capability for CV Builder",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "shell:allow-open"
  ]
}
