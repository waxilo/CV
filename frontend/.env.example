# 生产构建：Cloudflare Worker 自定义域名
VITE_API_BASE=https://cv-api.sloan.dpdns.org

# ---- 仅本地开发用（复制到 .env.local，该文件不入库）----
# dev server 把 /api 转发到哪里。不设则默认 http://127.0.0.1:8787（本地 wrangler dev）。
# 想直接调线上后端而不启本地后端，就设成线上域名：
# VITE_DEV_API_TARGET=https://cv-api.sloan.dpdns.org
#
# 注意：本地开发不要设置 VITE_API_BASE，否则 axios 会带绝对地址直连，
# 绕过 dev proxy 并触发跨域预检。留空让请求走相对路径 /api 即可。
